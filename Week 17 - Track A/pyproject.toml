[project]
name = "week17-track-a-churn"
version = "0.1.0"
description = "Week 17 Track A: Telco churn MLOps (train, track, register, serve, monitor drift)"
requires-python = ">=3.12,<3.13"
dependencies = [
    "scikit-learn>=1.5",
    "pandas>=2.2",
    "numpy>=1.26",
    "matplotlib>=3.9",
    "mlflow>=2.18,<3",
    "evidently>=0.6",
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "joblib>=1.4",
    "pydantic>=2",
]

[tool.uv]
package = false
